// queries exercice dia 1
use('class');

// Exerc-1 db.bios.find({_id: 8});

// Exerc-2 db.bios.find({_id: 8}, { _id: 1 })

// Exerc-3 db.bios.find({_id: 8}, { _id: 1, name: 1, _id: 0})

// Exerc-4 db.bios.find({ "name.first": "John" }).pretty()

// Exerc-5 db.bios.find().limit(3).pretty()

// Exerc-6 db.bios.find().limit(2).skip(5)
